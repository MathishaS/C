#include <stdio.h>
int main() {
	
    int n,num,sum=0;
    scanf("%d", &n);
    
    if(10000<=n && n<=99999)
    {
        while(n>0)
        {
            num=n%10;
            sum+=num;
            n=n/10;
        }
    
    printf("%d",sum);
    }
    
    return 0;
}
