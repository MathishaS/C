#include <stdio.h>
 int main()
{
  int n,count=0;
  scanf("%d",&n);
  int arr[n];
  for(int i=0;i<n;i++)
  {
     scanf("%d",&arr[i]) ;
  }
  for(int i=0;i<n;i++)
  {
     if(arr[i]!=0)
      {
          printf("%d ",arr[i]);
      }
      else
      {
          count++;
      }
  }
  for(int i=1;i<=count;i++)
  {
      printf("%d ",0);
  }
    return 0;
}
